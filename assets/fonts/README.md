# Fonts

Place **Roundex.otf** in this folder. The app will use it as the primary font (black, white, grey and green theme).

If the file is missing, the app may fail to build; add `Roundex.otf` here and restart the dev server.
