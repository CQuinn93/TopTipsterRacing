# Build and deploy the web app to GitHub Pages.
# Requires: Repository secrets SUPABASE_URL and SUPABASE_ANON_KEY (anon/public key, not service key).
# For LMS Web Push: also set VAPID_PUBLIC_KEY (public half only; private key stays server-side).
# In repo Settings → Pages, set Source to "GitHub Actions".
name: Deploy web to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/deploy-web.yml'
      - 'app/**'
      - 'lib/**'
      - 'components/**'
      - 'contexts/**'
      - 'constants/**'
      - 'assets/**'
      - 'scripts/inject-csp-meta.js'
      - 'scripts/spa-github-pages-fallback.js'
      - 'scripts/write-web-manifest.js'
      - 'public/**'
      - 'app.json'
      - 'package.json'
      - 'package-lock.json'
      - 'babel.config.js'
      - 'metro.config.js'
      - 'global.css'
      - 'app.config.js'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: 'pages'
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - run: npm ci

      - name: Build web
        run: npm run build:web
        env:
          EXPO_PUBLIC_SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          EXPO_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
          EXPO_PUBLIC_WEB_BASE_URL: ''
          EXPO_PUBLIC_VAPID_PUBLIC_KEY: ${{ secrets.VAPID_PUBLIC_KEY }}

      - name: Add .nojekyll (required so _expo folder is served)
        run: touch dist/.nojekyll

      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - id: deploy
        uses: actions/deploy-pages@v4
